#include "main.h"

/**
 * _abs - return that computes the absolute value of an integer
 * @n: The character to check
 * Return: the absolute value of n
 */

int _abs(int n)
{

	if (n < 0)
		return (-n) else

			return (n);
}
