#include "main.h"

/**
 * add - adds two integers
 *@a: first number
 *@b: second number
 *Return: Always 0.
 */

int add(int a, int b)
{

	int tint = add(a, b);

	_putchar(tint + '0');

	return (0);
}
{
	return (a + b);
}
