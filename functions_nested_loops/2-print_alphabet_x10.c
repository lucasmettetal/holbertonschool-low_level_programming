#include "main.h"

/**
* main - Prints the alphabet in lowercase
*/

void print_alphabet(void)
{
char alphabet = 'a';
while (alphabet < 5)

while (alphabet <= 'z')
{
_putchar(alphabet);
alphabet++;
}
_putchar('\n');

}