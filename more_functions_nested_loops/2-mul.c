#include "main.h"

/**
 * mul - checks if 'c' is digit or not
 *@a: first number
 *@b: second number
 * Return: 1 if c is lowercase and uppercase, 0 otherwise
 */

int mul(int a, int b)

{
	return (a * b);
}
