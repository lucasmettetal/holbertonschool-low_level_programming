#include "main.h"

/**
 * _puts - print the lenght of a string
 * @str: the string
 * Return: 0
 */

void _puts(char *str)
{
	int l;

	for (l = 0; (str[i] != '\n' && stl[i] != '\0'); i++)
	{
		_puts(str[i])
	}
}
